# cloudsmith-agent

> **NOTE:** This repo will be renamed to `cloudsmith-agent` (AB#1664) — Agent is the per-host Windows Service per ADR-007 2026-05-23 Relay/Agent/PSRemote split. The PaaS-site bridge is a separate component (`cloudsmith-relay`).

CloudSmith Agent daemon (Windows Service) — mTLS enrollment, job dispatch, and WinRM/Redfish payload execution.
